server:
  port: 8085

spring:
  application:
    name: notification-api
  data:
    redis:
      host: localhost
      port: 6379
  kafka:
    bootstrap-servers: localhost:9092
    consumer:
      group-id: notification-api
